import { IdSplitterPipe } from './id-splitter.pipe';

describe('IdSplitterPipe', () => {
  it('create an instance', () => {
    const pipe = new IdSplitterPipe();
    expect(pipe).toBeTruthy();
  });
});
