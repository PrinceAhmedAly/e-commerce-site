import { DateFromIDPipe } from './date-from-id.pipe';

describe('DateFromIDPipe', () => {
  it('create an instance', () => {
    const pipe = new DateFromIDPipe();
    expect(pipe).toBeTruthy();
  });
});
